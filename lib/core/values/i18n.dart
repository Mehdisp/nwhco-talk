import 'package:flutter/material.dart';

const Map<String, String> supportedLanguages = {
  'en': 'English',
};

final Map<Locale, Map<String, String>> supportedLocales = {
  const Locale('en', 'US'): {'en': 'English', 'native': 'English'},
};

final Map<Locale, String> languagesMap = {
  const Locale('en', 'US'): "English",
};

final Map<Locale, String> allSupportedLanguages = {
  const Locale('en', 'US'): "English",
};
